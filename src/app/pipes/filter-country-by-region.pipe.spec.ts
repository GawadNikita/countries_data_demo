import { FilterCountryByRegionPipe } from './filter-country-by-region.pipe';

describe('FilterCountryByRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCountryByRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
